# 1. 가상환경 (제일 중요! 뚱뚱해서 절대 올리면 안 됨)
.env
.venv/
venv/
env/

# 2. 파이썬이 자동으로 만드는 임시 파일들 (필요 없음)
__pycache__/
*.pyc

# 3. 에디터 설정 파일 (내 컴퓨터 설정이 남한테 가면 안 됨)
.idea/
.vscode/

# 4. (중요) 운영체제별 쓰레기 파일
.DS_Store
Thumbs.db

# 5. (선택) 만약 도커 데이터를 로컬 폴더에 저장한다면
pg_data/
postgres_data/